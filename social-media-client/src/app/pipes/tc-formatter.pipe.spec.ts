import { TcFormatterPipe } from './tc-formatter.pipe';

describe('TcFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TcFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
