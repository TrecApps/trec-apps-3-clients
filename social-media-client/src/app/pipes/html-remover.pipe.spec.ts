import { HtmlRemoverPipe } from './html-remover.pipe';

describe('HtmlRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
